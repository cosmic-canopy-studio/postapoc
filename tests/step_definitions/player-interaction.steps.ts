import { Given, Then, When } from '@cucumber/cucumber';

Given(/^a player focused on a bench$/, function () {
    return 'pending';
});

When(/^the player attacks the bench (.*) times$/, function () {
    return 'pending';
});
Then(/^the bench should have a healthBar$/, function () {
    return 'pending';
});
Then(/^the bench should have (.*) health left$/, function () {
    return 'pending';
});
Then(/^the bench should be (.*)$/, function () {
    return 'pending';
});
